[opt_def -cname [arg boolean]]

If [const false] (the default), a name for the corresponding C function is
automatically derived from the fully-qualified [arg tclname].  Otherwise, name
of the C function is the last component of [arg tclname].
