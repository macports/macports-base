/* ---------------------------------------------------------------------- */

#define ns__aproc0 "::aproc"
static void c__aproc0()
{

}

static int
tcl__aproc0(ClientData cd, Tcl_Interp *interp, Tcl_Size oc, Tcl_Obj *CONST ov[])
{

  if (oc != 4) {
    Tcl_WrongNumArgs(interp, 4, ov, NULL);
    return TCL_ERROR;
  }

  /* Call - - -- --- ----- -------- */
  c__aproc0();

  /* (void return) - - -- --- ----- -------- */
	return TCL_OK;
}
