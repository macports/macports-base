/* ---------------------------------------------------------------------- */

#define ns__aproc_beta0 "::aproc+beta"
static void c__aproc_beta0()
{

}

static int
tcl__aproc_beta0(ClientData cd, Tcl_Interp *interp, Tcl_Size oc, Tcl_Obj *CONST ov[])
{

  if (oc != 1) {
    Tcl_WrongNumArgs(interp, 1, ov, NULL);
    return TCL_ERROR;
  }

  /* Call - - -- --- ----- -------- */
  c__aproc_beta0();

  /* (void return) - - -- --- ----- -------- */
	return TCL_OK;
}
